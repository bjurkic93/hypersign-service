package com.reddiax.rdxvideo.constant;

public enum AdvertisementMediaTypeEnum {
    VIDEO,          // Single video (legacy, backward compatible)
    IMAGE_SET,      // Multiple images only
    VIDEO_SET,      // Multiple videos only
    MIXED_PLAYLIST  // Combination of videos and images
}
